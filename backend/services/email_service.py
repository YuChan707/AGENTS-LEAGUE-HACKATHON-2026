"""Email draft service.

Generates a follow-up email draft summarizing the session using a Llama 70B
model. Output feeds the Outlook-styled card in the frontend.

TODO (scaffold): implement draft_email(session) -> {subject, body}.
"""
