"""Ingestion service.

Persists raw streaming events and the derived analytics records to the
database (see backend.models.database and dtos.analytics).

TODO (scaffold): implement write_event(...) and write_analytics(...).
"""
