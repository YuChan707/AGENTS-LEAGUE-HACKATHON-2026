"""PPTX report generator.

Builds a PowerPoint session report (scores, highlights, recommendations)
with python-pptx. The resulting file is typically uploaded via blob_service.

TODO (scaffold): implement build_report(session) -> path/bytes.
"""
