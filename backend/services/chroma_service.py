"""ChromaDB service.

Owns the ChromaDB client: seeds the cultural-knowledge collection on startup
and exposes query helpers used by the cultural agent (RAG).

TODO (scaffold): implement get_client(), seed(), and query(text, k).
"""
