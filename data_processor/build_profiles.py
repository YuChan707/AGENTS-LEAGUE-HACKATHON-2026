"""Build audience profiles from raw stats.

Converts the raw Data Commons stats (plus synthetic LLM enrichment) into a
list of AudienceProfile (dtos.audience).

TODO (scaffold): implement build_profiles(raw_stats) -> list[AudienceProfile].
"""
